# GitHub OAuth App client_id (Device Flow enabled)
# Register at https://github.com/settings/developers
# Set this env var before `npm run tauri build` to embed in the binary.
GITHUB_CLIENT_ID=Ov23li_your_app_id
